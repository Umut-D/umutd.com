language: ruby
cache: bundler
rvm:
  - 2.6.3

install:
  # Install Jekyll and dependencies
  - bundle install

script: bundle exec rake site:deploy --quiet

branches:
  only:
  - master

notifications:
  email: false